{
  "_comment": "Items that trigger the chain tree-fell when used to break a log. Read by the plugin at startup (config.yml fallback: tree-felling.tools). Defaults to all axes; add custom RPG tool ids here.",
  "values": [
    "#minecraft:axes"
  ]
}
